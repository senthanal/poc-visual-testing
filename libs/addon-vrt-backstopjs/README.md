# addon-vrt-backstopjs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build addon-vrt-backstopjs` to build the library.

## Running unit tests

Run `nx test addon-vrt-backstopjs` to execute the unit tests via [Jest](https://jestjs.io).
